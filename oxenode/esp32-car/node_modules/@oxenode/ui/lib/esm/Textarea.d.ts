import React, { PropsWithChildren, CSSProperties } from "react";
type SliderProps = PropsWithChildren<{
    name: string;
    nodeId: string;
    value?: string;
    width?: string;
    language?: string;
    style?: CSSProperties;
}>;
export default function Textarea({ name, nodeId, value, width, language, style, }: SliderProps): React.JSX.Element;
export {};
