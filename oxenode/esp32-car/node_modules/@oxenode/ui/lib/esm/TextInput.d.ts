import React from "react";
type TextInputProps = React.PropsWithChildren<{
    style: React.CSSProperties;
    title?: string;
    name: string;
    nodeId: string;
    value?: string;
}>;
export default function TextInput({ style, title, name, nodeId, value, }: TextInputProps): React.JSX.Element;
export {};
