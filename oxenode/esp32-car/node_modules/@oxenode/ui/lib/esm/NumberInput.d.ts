import React from "react";
type NumberInputProps = React.PropsWithChildren<{
    style?: React.CSSProperties;
    title?: string;
    name: string;
    nodeId: string;
    value?: string;
}>;
export default function NumberInput({ title, name, value, nodeId, style }: NumberInputProps): React.JSX.Element;
export {};
