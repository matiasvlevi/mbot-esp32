import React from "react";
type SliderProps = {
    title?: string;
    name: string;
    value: string;
    min?: number;
    max?: number;
    step?: number;
    nodeId: string;
};
export default function Slider({ min, max, step, name, nodeId, value, }: SliderProps): React.JSX.Element;
export {};
