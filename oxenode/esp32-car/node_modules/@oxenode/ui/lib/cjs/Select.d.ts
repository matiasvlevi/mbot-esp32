import React from "react";
type SliderProps = React.PropsWithChildren<{
    nodeId: string;
    name: string;
    value: string;
    onChange?: (e: React.FormEvent<HTMLSelectElement>) => void;
}>;
export default function Select({ nodeId, name, value, children, onChange }: SliderProps): React.JSX.Element;
export {};
