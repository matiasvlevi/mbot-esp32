"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.disconnectEdge = void 0;
function disconnectEdge(nodeState, edge, direction) {
    // Disconnect where the edge comes from
    if (edge[direction]) {
        const port = nodeState[edge[direction].nodeId].ports.find((p) => p.id === edge[direction].portId);
        console.log('edge to remove: ', edge);
        console.log('disconnect at: ', port);
        if (port) {
            // Remove reference to edge in start port
            if (port.edgeIds) {
                port.edgeIds = port.edgeIds.filter((edgeId) => edgeId !== edge.id);
            }
            console.log('port with disconnection: ', port);
        }
    }
}
exports.disconnectEdge = disconnectEdge;
