import { Dispatch } from "react";
import { EdgeAction } from './reducers/reducers';
import { TEdge } from './types';
export interface TEdgeContextProps {
    edgeState: Record<string, TEdge>;
    dispatchEdge: Dispatch<EdgeAction>;
}
export declare const EdgeContext: import("react").Context<TEdgeContextProps>;
