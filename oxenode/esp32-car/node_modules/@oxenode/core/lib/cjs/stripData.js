"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stripData = void 0;
function stripData(data) {
    const removeId = (obj) => {
        const value = { ...obj };
        delete value.id;
        return value;
    };
    const removeVisualRep = (edge) => {
        const value = { ...edge };
        delete value.end;
        delete value.start;
        return value;
    };
    const nodes = Object.entries(data.nodes).reduce((acc, cur) => {
        acc[cur[0]] = removeId(cur[1]);
        return acc;
    }, {});
    const edges = Object.entries(data.edges).reduce((acc, cur) => {
        acc[cur[0]] = removeVisualRep(removeId(cur[1]));
        return acc;
    }, {});
    return { nodes, edges };
}
exports.stripData = stripData;
