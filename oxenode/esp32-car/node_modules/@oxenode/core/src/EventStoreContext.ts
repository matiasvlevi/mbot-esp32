import { Dispatch, createContext } from "react";

import { EventStoreAction } from './reducers/reducers'

export interface TEventStoreContextProps {
  eventStore: Record<string, any>;
  setEventStoreItem: Dispatch<EventStoreAction>;
}

export const EventStoreContext = createContext<TEventStoreContextProps>({ 
  eventStore: {},
  setEventStoreItem: () => {}
});