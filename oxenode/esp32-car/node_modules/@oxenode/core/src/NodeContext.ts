import { Dispatch, createContext } from "react";

import { NodeAction } from './reducers/reducers'
import { TNode } from './types'

export interface TNodeContextProps {
  nodeState: Record<string, TNode>;
  dispatchNode: Dispatch<NodeAction>;
}

export const NodeContext = createContext<TNodeContextProps>({ 
  nodeState: {},
  dispatchNode: () => {}
});